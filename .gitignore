# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Song generated during cache test
test_song.mp3

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
